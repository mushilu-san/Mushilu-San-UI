---
"@mushilu-san/ui": patch
---

Accessibility & UX polish across primitives, forms, and navigation: button/ghost/destructive/secondary `:active` states, `aria-busy`-driven loading, `touch-action: manipulation` and tap-highlight resets on form controls, dark-mode select arrow contrast, Avatar `aria-label` fallback to `name`, vertical Tab active indicator, and reduced-motion spinner fallbacks (pulse instead of freeze). Fixes Badge danger-variant text contrast to meet WCAG AA on its subtle background.
